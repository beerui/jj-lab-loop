# L1-S10 same-session thread refuses a second init

Mechanical. cwd = `$JJ_LAB_LOOP_ROOT/_materialized/loop-gym`.

A live run with `host.thread_id` (or CLI `--thread-id`) already exists. A second `init` in the same session must throw `same session already has live Ralph <run_id>`. Resume; do not invent a sibling slug.
