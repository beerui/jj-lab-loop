# L1-S9 review-slice is not a new requirement

Mechanical. cwd = `$JJ_LAB_LOOP_ROOT/_materialized/loop-gym`.

`init` of `task-*-review-fix` / title「审查修复」「按审查」must throw without `--force` / `new_requirement`. Resume the live feature run. `index.md` may write `## 同需求提示`; never merge or abandon from the prompt.
