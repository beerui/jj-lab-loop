# L1-S1 standard write-then-read

Agent-only. cwd = `$JJ_LAB_LOOP_ROOT/_materialized/loop-gym`.

Omit episode `role`. Label `lab_role:loop-gym`.

Prompt: persist note title so `get(id)` and `list()` agree. Fill acceptance with `must_id` / `evidence_class=write-then-read` and token `write_then_read:mock_ok`. Then `$jj-ralph` through ACCEPT/ARCHIVE. Resume with `reason`.
